# Ruckard Strategies

## docker-compose.yml

Instead of:

```
      --strategy FSimpleStrategy
```

you can use:

```
      --strategy-path /freqtrade/user_data/strategies/ruckard
      --strategy Trafilering
```

.
